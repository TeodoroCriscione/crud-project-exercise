{% extends 'base.html.twig' %}

{% block title %}New Recipe{% endblock %}

{% block body %}
<div class="container">
    <h1>Create new Recipe</h1>
    {# <div class="d-flex"> #}
        {{ include('recipe/_form.html.twig') }}
        <a class="btn btn-secondary" href="{{ path('app_recipe_index') }}">Back to list</a>
    {# </div> #}
</div>
{% endblock %}
